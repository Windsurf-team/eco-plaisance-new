---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const baseUrl = import.meta.env.BASE_URL;
const mareeUrl = (fileName: string) => `${baseUrl}marees/${fileName}`;
---

<Layout title="Horaires | E-co Plaisance">
    <Header />

    <!-- Hero Section specific for Hours -->
    <section class="hero-container" style="min-height: 60vh; background: radial-gradient(circle at center, #1a4a5a 0%, #0A1A2B 100%);">
        <div class="hero-content">
            <p class="handwritten hero-subtitle">Port du Teich, Bassin d'Arcachon</p>
            <h1 class="hero-title">HORAIRES</h1>
            <p class="hero-lead">Les marées ouvrent de belles sorties au lever et au coucher du soleil</p>
        </div>

        <div class="wave-separator wave-bottom">
            <svg viewBox="0 0 1440 320" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
                <path class="fill-white" fill-opacity="1" d="M0,224L48,213.3C96,203,192,181,288,181.3C384,181,480,203,576,224C672,245,768,267,864,261.3C960,256,1056,224,1152,197.3C1248,171,1344,149,1392,138.7L1440,128L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path>
            </svg>
        </div>
    </section>

    <section class="details-section" style="padding-top: 100px;">

        <!-- Horaires de navigation -->
        <div class="detail-block" style="grid-template-columns: 1fr;">
            <div class="detail-text" style="max-width: 1100px; margin: 0 auto;">
                <span class="handwritten" style="color: var(--teal-accent); font-size: 2rem;">Horaires</span>
                <h3>Horaires de navigation</h3>
                <p>Les horaires de navigation en bateaux sont contraints par les horaires des marées que vous pouvez télécharger dans les documents ci-dessous, en fonction de la date qui vous convient.</p>
            </div>
        </div>

        <!-- Paddle -->
        <div class="detail-block">
            <div class="detail-text">
                <span class="handwritten" style="color: var(--teal-accent); font-size: 2rem;">Paddle</span>
                <h3>Paddle</h3>
                <p>Concernant les Paddle, les sorties peuvent se prévoir toute la journée entre <strong>8h et 21h</strong>.</p>
                <a href="#reservation" class="cta-big" style="margin-top: 20px; font-size: 0.9rem; padding: 15px 30px;">Réserver</a>
            </div>
            <div class="detail-image" style="background-image: url('https://images.unsplash.com/photo-1528909514045-2fa4ac7a08ba?q=80&w=2000&auto=format&fit=crop');"></div>
        </div>

        <!-- Téléchargement marées -->
        <div class="detail-block" style="grid-template-columns: 1fr;">
            <div class="detail-text" style="max-width: 1100px; margin: 0 auto; text-align: center;">
                <span class="handwritten" style="color: var(--teal-accent); font-size: 2rem;">Documents</span>
                <h3>Téléchargez les horaires des marées</h3>
                <p>Sélectionnez le mois de votre choix pour consulter les horaires de navigation disponibles.</p>

                <div class="month-grid" aria-label="Téléchargement des horaires des marées par mois">
                    <a class="month-btn" href={mareeUrl('AVRIL-2026.png')} target="_blank" rel="noopener">AVRIL</a>
                    <a class="month-btn" href={mareeUrl('MAI-2026.png')} target="_blank" rel="noopener">MAI</a>
                    <a class="month-btn" href={mareeUrl('JUIN-2026.png')} target="_blank" rel="noopener">JUIN</a>
                    <a class="month-btn" href={mareeUrl('JUILLET-2026.png')} target="_blank" rel="noopener">JUILLET</a>
                    <a class="month-btn" href={mareeUrl('AOUT-2026.png')} target="_blank" rel="noopener">AOÛT</a>
                    <a class="month-btn" href={mareeUrl('SEPTEMBRE-2026.png')} target="_blank" rel="noopener">SEPT.</a>
                    <a class="month-btn" href={mareeUrl('OCTOBRE-2026.png')} target="_blank" rel="noopener">OCT.</a>
                </div>
            </div>
        </div>

    </section>

    <Footer />
</Layout>

<script>
    // Animation d'entrée du titre
    window.addEventListener('load', () => {
        const heroTitle = document.querySelector('.hero-title') as HTMLElement;
        if(heroTitle) {
            heroTitle.style.opacity = '1';
            heroTitle.style.transform = 'translateY(0)';
        }

        const heroSubtitle = document.querySelector('.hero-subtitle') as HTMLElement;
        if(heroSubtitle) {
            heroSubtitle.style.opacity = '1';
            heroSubtitle.style.transform = 'translateY(0)';
        }
        const heroLead = document.querySelectorAll('.hero-lead');
        heroLead.forEach((el) => {
             (el as HTMLElement).style.opacity = '1';
             (el as HTMLElement).style.transform = 'translateY(0)';
        });
    });
</script>

<style>
    .month-grid {
        margin-top: 22px;
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 12px;
    }

    .month-btn {
        display: inline-block;
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-weight: 700;
        padding: 14px 18px;
        border-radius: 14px;
        background: var(--deep-water);
        color: var(--white);
        border: 1px solid rgba(10, 26, 43, 0.12);
        transition: transform 0.15s ease, background 0.2s ease, color 0.2s ease;
    }

    .month-btn:hover {
        transform: translateY(-1px);
        background: var(--sand);
        color: var(--deep-water);
    }

    @media (min-width: 768px) {
        .month-grid {
            grid-template-columns: repeat(4, minmax(0, 1fr));
        }
    }
</style>
